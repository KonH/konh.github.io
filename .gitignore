.DS_Store
node_modules
/dist

# Build-only source for the generated CV PDF (see src/scripts/generatePdf.ts).
# The deployed copy at the repo root (Konstantin_Khitrykh_CV.pdf) is the one
# actually committed/served; this is just the build intermediate, so it
# isn't tracked separately to avoid committing the same PDF bytes twice.
/public/Konstantin_Khitrykh_CV.pdf


# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/settings.local.json
